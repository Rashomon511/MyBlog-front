import {connect} from 'react-redux';
import TagsMange from '../../components/Backend/TagsMange/TagsMange';

function mapStateToProps() {
    return {};
}

function mapDispatchToProps() {
    return {};
}


export default connect(mapStateToProps, mapDispatchToProps)(TagsMange);